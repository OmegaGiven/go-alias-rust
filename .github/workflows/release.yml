name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x86_64
            os: ubuntu-latest
            archive: OGdevDesk-server-linux-x64.tar.gz
            package_dir: OGdevDesk-server-linux-x64
            binary: ogdevdesk_service
            install_script: install-linux.sh
          - name: macos-arm64
            os: macos-latest
            archive: OGdevDesk-server-macos-arm64.tar.gz
            package_dir: OGdevDesk-server-macos-arm64
            binary: ogdevdesk_service
            install_script: install-macos.sh
          - name: windows-x86_64
            os: windows-latest
            archive: OGdevDesk-server-windows-x64.zip
            package_dir: OGdevDesk-server-windows-x64
            binary: ogdevdesk_service.exe
            install_script: install-windows.ps1

    steps:
      - uses: actions/checkout@v4

      - uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Build release binary
        run: cargo build --release --locked

      - name: Prepare package
        shell: bash
        run: |
          mkdir -p "dist/${{ matrix.package_dir }}"
          cp "target/release/${{ matrix.binary }}" "dist/${{ matrix.package_dir }}/"
          cp README.md "dist/${{ matrix.package_dir }}/"
          cp -R static "dist/${{ matrix.package_dir }}/static"
          if [[ "${{ runner.os }}" == "Windows" ]]; then
            cp scripts/install-local-windows.ps1 "dist/${{ matrix.package_dir }}/${{ matrix.install_script }}"
          else
            cp scripts/install-local-unix.sh "dist/${{ matrix.package_dir }}/${{ matrix.install_script }}"
            chmod +x "dist/${{ matrix.package_dir }}/${{ matrix.install_script }}"
          fi

      - name: Package Unix archive
        if: runner.os != 'Windows'
        shell: bash
        run: |
          cd dist
          tar -czf "${{ matrix.archive }}" "${{ matrix.package_dir }}"

      - name: Package Windows archive
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Compress-Archive -Path "dist/${{ matrix.package_dir }}" -DestinationPath "dist/${{ matrix.archive }}"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.archive }}
          path: dist/${{ matrix.archive }}

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          files: dist/*
          generate_release_notes: true
